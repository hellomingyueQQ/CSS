输入：
        尚硅谷视频 
        中文网手册看一遍

搭建环境：
      1. 建立ts文件，直接命令行 tsc xxx.ts,生成js
            tsc xx.ts -w
      2. code runner，全局需要安装ts-node npm install -g ts-node
      3. 通过配置文件，编译文件,tsconfig.json

目标：
      项目typescript代码和配置，全都能看懂

下一步：
     尚硅谷第二个阶段配置部分
     看中文网视频，敲代码